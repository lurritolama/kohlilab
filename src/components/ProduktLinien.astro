---
// Zentrale Produktlinien-Übersicht. Neue Konfiguratoren/Linien hier
// ergänzen — jede Linie bekommt eine eigene Unterseite.
const linien = [
  {
    slug: 'qr-schilder',
    name: 'QR-Schilder',
    status: 'test',
    beschreibung:
      'Dein QR-Schild im Konfigurator: Form, Farben, WLAN/URL/vCard, Text und Logo — mehrfarbig 3D-gedruckt.',
    href: '/qr-schilder',
  },
  {
    slug: 'organizer',
    name: 'Schubladen-Organizer',
    status: 'bereit',   // kein Badge mehr — Konfigurator ist fertig und bestellbar
    beschreibung:
      'Dein Schubladeneinsatz nach Mass: Innenmass eingeben, Fächer frei einteilen — für Besteck, Büro, Werkzeug und mehr.',
    href: '/organizer',
  },
  {
    slug: 'ventilkappen',
    name: 'Ventilkappen',
    status: 'live',
    beschreibung:
      'Kleine Skulpturen mit Ventilgewinde aus der KI-Mikrofabrik — Schrader oder Presta, neue Sujets laufend, jedes von Hand freigegeben.',
    href: '/ventilkappen',
  },
  {
    slug: 'spezialanfertigung',
    name: 'Spezialanfertigung',
    status: 'live',
    beschreibung:
      'Ersatzteil, Halterung, Einzelstück oder dein fertiges 3D-Modell: Anfrage schicken, wir prüfen kostenlos die Machbarkeit.',
    href: '/spezialanfertigung',
  },
];
---
<section class="block" id="produkte">
  <h2>Produktlinien</h2>
  <p class="lead">Alles aus einer Werkstatt — jede Linie mit eigenem Konfigurator oder eigener Fabrik dahinter.</p>
  <div class="cards">
    {linien.map((l) => (
      <a class={`card linie ${l.href ? '' : 'stumm'}`} href={l.href ?? undefined}>
        <div class="thumb linie-thumb">
          {l.status === 'live' && <span class="tag">LIVE</span>}
          {l.status === 'test' && <span class="tag tag-test">TEST</span>}
          {l.status === 'bald' && <span class="tag tag-dim">BALD</span>}
          {l.slug === 'ventilkappen' ? (
            <img src="/bilder/ventilkappen-titel.jpg" alt="Vier frisch gedruckte Glace-Ventilkappen auf dem Druckbett" loading="lazy" width="820" height="512" />
          ) : l.slug === 'organizer' ? (
            <img src="/bilder/organizer-titel.jpg" alt="Gedruckter Schubladen-Organizer mit Trennwänden auf dem Druckbett" loading="lazy" width="930" height="581" />
          ) : l.slug === 'qr-schilder' ? (
            <img src="/bilder/qr-schilder-titel.jpg" alt="Neun frisch gedruckte QR-Marker auf dem Druckbett" loading="lazy" width="576" height="360" />
          ) : l.status === 'bald' ? (
            <svg width="84" height="100" viewBox="0 0 90 110"><rect x="20" y="30" width="50" height="50" rx="6" fill="none" stroke="#9a9a93" stroke-width="4" stroke-dasharray="8 6"/><text x="45" y="63" text-anchor="middle" fill="#9a9a93" font-size="26" font-family="monospace">?</text></svg>
          ) : (
            <svg width="84" height="100" viewBox="0 0 90 110"><path d="M45 25 v40 M25 45 h40" stroke="#ff6b1a" stroke-width="5" stroke-linecap="round"/><circle cx="45" cy="45" r="34" fill="none" stroke="#e8e6e1" stroke-width="4"/></svg>
          )}
        </div>
        <div class="meta">
          <div>
            <div class="nm">{l.name}</div>
            <div class="ds">{l.beschreibung}</div>
          </div>
        </div>
      </a>
    ))}
  </div>
</section>
